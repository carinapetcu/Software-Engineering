package iss.core.repository;

import iss.core.domain.CoChair;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CoChairRepository extends JpaRepository<CoChair,Long> {
}
